import 'package:flutter/material.dart';

class LikePost extends StatelessWidget {
  final bool isLiked;
  LikePost({this.isLiked});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
