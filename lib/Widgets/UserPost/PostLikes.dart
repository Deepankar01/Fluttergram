import 'package:flutter/material.dart';

class PostLikes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: <Widget>[Text('Counts'), Text('Likes')],
    );
  }
}
